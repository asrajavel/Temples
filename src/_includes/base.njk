<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/png" href="{{ '/images/favicon.png' | url }}">
  <meta name="description" content="{% if temple %}{{ temple.name }} - {{ temple.location }}. {{ temple.moolavar }}{% else %}A collection of sacred temple shrines{% endif %}">
  <title>{% if temple %}{{ temple.name }}{% else %}{{ title }}{% endif %} | Divya Desams</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Source+Serif+4:opsz,wght@8..60,400;8..60,600&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
</head>
<body>
  {{ content | safe }}
</body>
</html>


---
layout: base.njk
title: "Divya Desams"
---

<div class="home-page">
  <header class="home-header">
    <h1>Divya Desams — Utsavams</h1>
    <p class="subtitle">Divya Desam Temples and their sacred festivals</p>
    <a href="{{ '/about/' | url }}" class="about-link">What are Divya Desams? →</a>
  </header>

  <div class="temples-grid">
    {% for temple in temples %}
    <a href="{{ '/temples/' | url }}{{ temple.slug }}/" class="temple-card">
      <div class="card-image" style="background-image: url('{{ temple.coverImage | url }}');"></div>
      <div class="card-content">
        <h2>{{ temple.name }}</h2>
        <p class="card-location">{{ temple.location }}</p>
        <p class="card-deity">{{ temple.moolavar }}</p>
      </div>
    </a>
    {% endfor %}
  </div>
</div>

